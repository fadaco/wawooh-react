import {
  // PROFILE_TAB
} from '../Constants';


const INITIAL_VALUES = {
  profileTab: 'landing'
};

const profileReducer = (state = INITIAL_VALUES, action) => {
  switch (action) {
    default:
      return state;
  }
};

export default profileReducer;